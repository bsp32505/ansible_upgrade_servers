---

- hosts: 192.168.88.26
  become: true
  tasks:

  - name: update repository index
    apt:
      update_cache: yes
    when: ansible_distribution == "Ubuntu"

  - name: upgrade packages
    apt:
      name: apache2
      state: absent
    when: ansible_distribution == "Ubuntu"
